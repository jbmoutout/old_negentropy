$('#dialogs').append('<%= j render "dialog" %>');
<% if @dialogs.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@dialogs) %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>