
<div class="padding-top">
  <div class="container">
    <div class="row">
      <p class="padding-bottom"><u>Collection</u></p>
      <div class="col-xs-3 col-xs-offset-9 col-sm-3 col-sm-offset-9">
        <div class="padding-top">
          <p>Artists: <% @dialogs.each do |t| %>
            <div class="row">
              <%= t.text.match(/(?<=: )(.*?),/) %>
            </div>
            <% end %>
          </p>
        </div>
      </div>
      <div class="stream-item-lt"></div>
      <% @dialogs.each do |d| %>
        <div class="col-xs-12 col-sm-4">
          <div class=" padding1">
            <div class="img-tweet-box">
              <img src="<%= d.img_url %>" class="img img-tweet"> 
            </div>
            <div class="img-tweet-box">
              <%= d.text.match(/(?<=: )(.*?),/) %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>