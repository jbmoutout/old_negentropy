<div id="dialogs">
  <div class="scroll-top">Back to top</div>
  <% cache("dialog") do  %>
    <%= render "dialog" %>
  <% end %>
</div>

<% if @dialogs.size > 0 %>
  <%= will_paginate @dialogs %>
<% end %>